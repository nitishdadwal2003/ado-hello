name: 'hello world'
on:
    workflow_dispatch:
jobs:
    hello-world:
        runs-on: ubuntu-latest
        steps:
            - run: |
                echo "Hello world"
                echo "namaste duniya"
              name: Hello world steps

