name: 'hello world'
on:
    workflow_dispatch:
jobs:
    hello-world:
        runs-on: gh-agent
        steps:
            - run: |
                echo "Hello world"
                echo "namaste duniya"
              name: Hello world steps

